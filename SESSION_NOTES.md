# Session Handoff — iOS Roblox UI Lib

Read this first in a new session to pick up exactly where this one left off. Project root: `C:\Users\theha\Downloads\chet\coding folder\project lib`.

## What this is

A Rayfield-Gen2-shaped Roblox UI library (`CreateWindow` → `CreateTab` → `CreateToggle`/`Slider`/`Dropdown`/`MultiDropdown`/`Input`/`Keybind`/`ColorPicker`/`Button`/`Label`/`ConfigManager`/`Section`), skinned in an Apple HIG dark-mode + glassmorphism look. Built 100% from `Instance.new`, zero assets. Also: draggable window (drag the header), a draggable floating button that shows/hides the whole menu (`Window:ToggleVisible`, also wireable to a `Flag`'d `Keybind`), `Library:Notify` toasts, `Library:SetTheme` (Dark/Light/custom palette, live re-theme), named `Library:SaveConfig`/`LoadConfig`/`ListConfigs`/`DeleteConfig` (JSON via `writefile`/`readfile`, keyed by each widget's optional `Flag`, multiple presets under `IOSRobloxUILib_Configs/`), and `CreateSection` for grouping widgets under a titled card (e.g. Settings' `Theme`/`Config` cards). See `MASTER.md` §7–15 for specs on all of this.

Also now a git repo, pushed to a **private** GitHub repo (`github.com/ruku-git/RukuLIB`, branch `main`) — see "Git / GitHub" section below for the distribution approach (pin `loadstring(game:HttpGet(...))` to a commit hash, don't track `main`).

## Files

- `library.lua` — the actual library, ship this. Every token traces to `MASTER.md`.
- `MASTER.md` — design-system source of truth: colors, radius, spacing, typography, motion, icons, dock-toggle spec. Read this before changing any visual value in `library.lua`.
- `preview.html` — standalone browser mock, open directly (no server). Kept in visual parity with `library.lua` (same icons, same dock-collapse behavior).
- `demo.lua` — **permanent** full-feature showcase (NOT a throwaway smoke test, don't delete it): one window, 4 tabs (Main/Visuals/Player/Settings), exercises every widget + every library-level feature (drag, Notify, SetTheme, SaveConfig/LoadConfig). Uses `readfile(PATH)` assuming the end user places `library.lua` in their own executor's workspace — swap to `game:HttpGet(...)` if hosting it remotely. Kept for showing the library to someone or sanity-checking a change against the whole surface at once. When testing THIS session's own changes, still build a throwaway `combined_smoke_test.lua` (inlining the real `library.lua` content, stripped of its trailing `return`) rather than trying to `execute-file` `demo.lua` directly — its `readfile(PATH)` call hits the same sandboxed-workspace limitation as everything else.
- `testhub.lua` — **permanent** (NOT a throwaway smoke test, don't delete it): unlike `demo.lua`, every control does something real to the local player/character instead of just printing. 4 tabs: **Character** (Appearance section: ColorPicker recolors every `BasePart`, Dropdown swaps `Material`; Movement section: Fly [Toggle+Slider+Keybind, BodyVelocity/BodyGyro driven by held WASD/Space/LeftControl relative to camera], Noclip [Toggle+Keybind, re-asserts `CanCollide=false` every `Stepped`], WalkSpeed and JumpPower [each a Toggle-gates-a-Slider pair — the slider always updates its own state, but only actually reaches the `Humanoid` while its toggle is on, reverting to the real default the instant it's switched off]), **Visuals** (MultiDropdown+ColorPicker driving a `Highlight` instance's fill/outline, Button to reset color/material/highlight), **Players** (Input filter + a hand-built roster of every player in the server — avatar circle via `GetUserThumbnailAsync` + `UICorner(1,0)`, name, UserId, Teleport button; live via `PlayerAdded`/`PlayerRemoving`), **Settings** (same Theme/Config `CreateSection` pattern as `demo.lua`). `LocalPlayer.CharacterAdded` re-applies every persistent effect (color/material/highlight/walkspeed/jumppower/noclip) on respawn. Same `readfile(PATH)` local-dev assumption as `demo.lua`.
- `ios_roblox_lib.excalidraw` — hand-authored wireframe JSON (there's no real Excalidraw MCP tool in this environment; the file was written directly as raw Excalidraw scene JSON). Static, from early planning — not kept in sync with later tweaks.

## Live-testing setup

- Roblox client connects via the `roblox-mcp` skill/tools (`mcp__roblox-mcp__*`). Load the skill, `list-clients`, `set-active-client`.
- **This user's Roblox runs on an Android phone**, mirrored to the PC via scrcpy (or similar) — there is no local Roblox window, so `list-roblox-windows` / `screenshot-window` never find anything. Screenshots have to come from the user (phone screenshot or a snip of the mirror window).
- **`mcp__screen-capture-mcp__take_screenshot` is broken** — confirmed bug, both full-screen and `window_title` paths fail identically with a PowerShell parse error (`es-ES` locale) because the tool builds its `-Command` string with literal `\n` text instead of real newlines. Already filed as feedback (draft queued via `SendFeedback`, not sent). Don't rely on it — ask the user for a screenshot instead.
- The client's `clientId`/job id changes across reconnects — don't assume a previously-used id is still valid; re-run `list-clients`.
- Workflow used for changes: write a throwaway `combined_smoke_test.lua` in the project dir that inlines the full current `library.lua` source (`readfile` on a real OS path fails — the executor's `readfile` is sandboxed to its own workspace, not the real filesystem) plus a smoke-test invocation, `execute-file` it, check `get-console-output` for `SMOKE_TEST_OK`/errors, optionally probe live instance state with `get-data-by-code`, then delete the temp file when done. Never leave the temp smoke-test file behind.
- **`library.lua` ends with a top-level `return Library`.** When building the smoke-test copy, do NOT just concatenate the whole file — strip that trailing `return Library` line first (`grep -n "^return Library$" library.lua` to find the line, then `head -n <line-1>`), otherwise the appended demo code becomes a statement after `return` in the same chunk, which is invalid Luau. This produces a confusing, consistent `attempt to call a nil value` error at a fixed line/script-id no matter what's appended — burned real time on this once, don't repeat it.
- The window's actual GUI parent varies run to run — it can land in `CoreGui`, the hidden-UI container (`gethui()`), or `PlayerGui` depending on which `pcall` branch in `getGuiParent()` succeeds that session. When probing live state, check all three rather than assuming `CoreGui`.

## Bugs found and fixed this session (all applied to `library.lua`)

1. **Duplicate windows on re-run.** `CreateWindow` used to just create a new `ScreenGui` every call with no cleanup — re-executing the script stacked windows on top of each other (looked like "the previous tab won't disappear," but was actually two whole separate windows overlapping). Fixed: `CreateWindow` now destroys every existing `IOSRobloxUILib` child in the gui parent before creating a new one.
2. **Square corners on the dock side.** `ClipsDescendants` on the outer `sheet` only clips to a rectangular bounding box — it does **not** respect a sibling's `UICorner` shape. The `dock` frame (flat rectangle, different color from `sheet`) poked sharp corners through the sheet's rounded silhouette. Fixed: gave `dock` its own matching `UICorner(Radius.Sheet)`.
3. Icons were empty placeholders → replaced with hand-built zero-asset vector glyphs (`IconBuilders.terminal/stack/sliders/dot` in `library.lua`), tinted active/inactive via `tintIcon()`. Numeric `rbxassetid` icons still supported as a fallback path.
4. Added the round dock-collapse toggle button (floats on `sheet`, not `dock`, so it survives collapse). Spec lives in `MASTER.md` §7.
5. **Dropdown flyout rendered underneath later rows (e.g. a `Test Button` below it).** Root cause: `screenGui.ZIndexBehavior` was `Sibling`, which scopes ZIndex comparisons to same-parent children only. The dropdown's flyout list (`ZIndex = 10`/`11`, nested inside its own row) and the dock-toggle glyphs (`ZIndex = 25-27`) were both written assuming *global* stacking, but under `Sibling` those numbers never got compared against an unrelated branch (a different row) — draw order fell back to child/creation order instead, so a later row could paint over an open dropdown regardless of its ZIndex. Fixed: `ZIndexBehavior` → `Enum.ZIndexBehavior.Global` (`library.lua` line ~298). Verified live via `execute-file` smoke test (Fly Hack toggle / Speed slider / Mode dropdown forced open / Test Button) — flyout now correctly draws on top. Screenshot confirmed via the MuMu emulator window (see capture workaround below).
6. **Clicking a dropdown option (e.g. "Noclip") also fired whatever was underneath the flyout (e.g. "Test Button").** First attempt: gave the flyout `list.Active = true` (Frames default `Active = false`, which doesn't block clicks to whatever they visually overlap). **This did NOT actually fix it** — user sent a screen recording re-testing with the `Active = true` build, still reproduced (Test Button fired when clicking Noclip). Real root cause: the flyout was nested *inside its own row* (parented to `btn`, inside `Row3`), which put it in the same branch of the instance tree as every other row, including Test Button's (`Row4`). Even fully-Active GuiButtons in sibling rows could apparently still both register a single click in that structure — `Active`/ZIndex tuning wasn't enough to fully separate them. **Real fix:** added a dedicated top-level `Overlay` frame (`Window.Overlay`, a child of `sheet`, created in `CreateWindow`) that floating UI parents into instead of nesting inside a row. `CreateDropdown`'s `list` now parents to `overlay`, not `btn`, with its position computed fresh on every open via `positionList()` (`btn.AbsolutePosition`/`AbsoluteSize` converted to overlay-local pixel offsets, since it no longer inherits `btn`'s position for free). This makes the flyout a fully separate branch from every row — there's no other row's button anywhere "behind" it in the tree for a click to also land on. `list.Active = true` kept as a belt-and-suspenders second layer. Also added in this pass (kept from the first attempt): the flyout animates open/close (height 0→full + fade, `Motion.Tab`, 160ms Quad Out) instead of an instant `Visible` flip, and the currently-selected option highlights in `AccentBlue` while open. New motion entries logged in `MASTER.md`. User confirmed live: **this one actually holds** — dropdown/Test Button conflict is gone.

7. **Regression: after fix #6, Fly Hack toggle stopped toggling and Test Button stopped responding/animating.** User caught this via real testing. Investigation used synthetic clicks (`mousemoveabs`/`mouse1click`, confirmed present in this executor) to try to reproduce it live — but a **control test on a completely bare `TextButton`, no window/library code involved at all, also failed to register a synthetic click**, proving the synthetic-click test method itself is unreliable in this environment (probably tracks the real OS cursor rather than actually warping it, so the earlier "verified with a real click" claim for bug #6 was likely a false positive from the user's own cursor happening to already be near the target, not real proof the tool works). Stopped trusting synthetic clicks as verification for the rest of this session.

   First attempted fix: reordered so `overlay` (added in bug #6) is created *before* `content` in `sheet`'s children, reasoning that `ZIndexBehavior.Global` breaks same-default-ZIndex ties by insertion order (later child wins) and `overlay` — created after content originally — may have been winning that tie and silently eating all clicks to content. **User confirmed live: this fixed the toggle/button regression.** But then:

8. **Regression: fixing #7 un-fixed #6** — user reported the dropdown-vs-Test-Button double-click conflict was back after the overlay/content reorder. This confirms the insertion-order tie-break theory from #7 was right, but also proves insertion order is not a safe knob to turn: overlay-after-content fixes #6 but breaks #7; overlay-before-content fixes #7 but un-fixes #6. They were never independent — flipping the order just traded one bug for the other. **Real fix:** stopped relying on insertion-order ties entirely. Gave `overlay` an explicit `ZIndex = 0` (below content's default `1`), so `overlay`'s own (invisible) body can never win input priority over `content` regardless of creation order — while a flyout's `list` (explicit `ZIndex = 10`, a child of `overlay` — children don't inherit a parent's ZIndex in Roblox) still unconditionally draws/receives input above content whenever it's actually open, since `10 > 1` regardless of order either way. This should be deterministic in a way the previous two attempts weren't. Verified only: zero runtime errors, and live property check confirms `Overlay.ZIndex == 0` while Toggle's `Track` and Test Button's own button both sit at the default `1` — **not yet confirmed by the user's own test**, since synthetic clicks aren't trustworthy here. This is the third attempt at this specific class of bug (rows #6/#7/#8) — flag to the user if a fourth surfaces, might mean the whole "shared overlay" approach needs rethinking rather than another tweak.

## Screenshot capture workaround (this session)

`mcp__screen-capture-mcp__take_screenshot` is still broken exactly as previously documented (es-ES locale chokes on the tool's literal `\n` in its PowerShell `-Command` string). Don't bother retrying it — instead run the equivalent capture directly via the `PowerShell` tool (real newlines, not string-literal `\n`):

- User's Roblox now runs through a **MuMu emulator** window, process name `MuMuNxDevice`, but its `MainWindowTitle` is `"Android Device"` — not `"MuMuNxDevice.exe"`. Target by `Get-Process -Name "MuMuNxDevice"`, not by title match.
- `GetWindowRect` via a `user32.dll` P/Invoke + `CopyFromScreen` into a `System.Drawing.Bitmap`, saved to a temp `.png` in the project dir, then read with the `Read` tool. Delete the temp png after.

9. **Real fix for the dropdown-vs-Test-Button click-through, found via the user's actual physical tap-test (screenshot: dropdown open, tapping "Noclip" also fired Test Button, and Noclip sometimes failed to select at all).** Root cause was different from bugs #6/#7/#8: `list` (the flyout background Frame) had `ZIndex = 10`, but the individual clickable option buttons (`opt`, the actual `TextButton` per option, `CreateDropdown` ~line 936) were created with **no explicit ZIndex**, defaulting to `1`. ZIndex is **not inherited from parent** in Roblox — a child does not adopt its parent's stacking value just because the parent is layered above everything. So `opt`'s real input-priority ZIndex (`1`) tied exactly with Test Button's own default-ZIndex `TextButton`. Under `ZIndexBehavior.Global`, tied ZIndex falls back to creation-order tie-break, which is nondeterministic-feeling in practice — explains both symptoms at once: sometimes Test Button won the tie (double-fire), sometimes the option button lost outright (Noclip not selecting). Fixed: gave `opt` an explicit `ZIndex = 10` (matching `list`) so it unambiguously outranks any row's default-ZIndex content regardless of creation order. Verified live via `get-data-by-code`: all three option `TextButton`s now report `ZIndex = 10`. **Not yet re-confirmed by the user's physical tap-test after this specific fix** — that's the next step.

   Side note on this investigation: before finding the real bug, a large chunk of time went into a red herring entirely of this session's own making. The smoke-test workflow (concatenate `library.lua` + demo code, `execute-file` it) kept throwing an identical `attempt to call a nil value` error at a fixed line/script-id regardless of what demo code was appended — including trivial no-op appends. Root cause: `library.lua` ends with a top-level `return Library` (last line), and in Lua/Luau nothing may follow a `return` in the same chunk. Every naive concatenation (raw copy of the file + appended lines) was therefore syntactically invalid. Fix for the *workflow*, not the library: strip the trailing `return Library` line (locate it with `grep -n "^return Library$"`, `head -n` up to but excluding it) before appending demo/test code. Recorded here so a future session doesn't burn time on the same false lead.

10. **Dock-collapse animation visually "detached" into what looked like a separate floating frame mid-tween.** User reported this from a phone screenshot; a synthetic instant-collapse probe (no tween, just snapping the end-state values) looked totally clean, which pointed at something transient happening only *during* the animation rather than a resting-state layout bug. Root cause: `dock`'s `UICorner` used a fixed 20px radius on all 4 corners (from bug #2's fix), but only the left two corners are ever near the sheet's rounded silhouette — the right two are a purely internal seam against `content` and never needed rounding. As `dock.Size.X` tweens down toward 0, a fixed 20px radius on a frame narrower than 40px forces Roblox to render it as a shrinking rounded pill/blob for the last stretch of the animation, which reads as a separate floating rounded shape detaching from the sheet. Fixed: captured the dock's `UICorner` as `dockCorner` and added a parallel tween on `CornerRadius` (20 → 0 closing, 0 → 20 opening) alongside the existing width tween, so the radius never exceeds half the frame's current size. Verified: smoke test loads clean, no runtime errors. **Not yet confirmed by the user actually watching the collapse animation** — that's the next thing to check.

11. **Dock read as a visually separate rounded panel "bolted onto" the sheet, not one continuous frame — this was the deeper issue behind the user's "coming from outside the main frame" report, distinct from bug #10's animation-only blob.** Two more zoomed user screenshots (dock open, resting state) made it clear: `corner(dock, Radius.Sheet)` rounds all 4 corners of `dock`, but the right two sit against `content` deep inside the window, nowhere near the sheet's outer edge — rounding them made `dock` render as its own independently-rounded rectangle. Fixed: added a same-color (`Colors.BgDock`) flat-cornered `RightSquareOff` patch frame, `Radius.Sheet` px wide, full height, positioned at `dock`'s rightmost strip (`Position = UDim2.new(1, -Radius.Sheet, 0, 0)`), parented inside `dock` after the corner. It paints over the right corners' curve so only the left two corners (the ones actually flush against the sheet's own rounding) stay visibly rounded. Verified: smoke test loads clean; self-captured screenshot (PowerShell/MuMu workaround) shows the dock/content boundary as a clean straight seam instead of a curved notch. **Not yet confirmed by the user's own eyes on the real device** — downscaled tool screenshots may miss subtle mismatches they'd catch directly.

12. **Regression from bug #11's own fix: tab slots rendered misaligned (dot pushed to the bottom of the dock instead of under the menu button)**, caught immediately by the user in their next round of screenshots. Root cause: `RightSquareOff` (added directly to `dock`) was a sibling of `dock`'s `UIListLayout` — and `UIListLayout` arranges *every* GuiObject child of its parent regardless of intent, no "ignore this one" flag exists. `RightSquareOff`'s full-height (`Size.Y.Scale = 1`) got treated as a giant first list item (default `LayoutOrder = 0`, sorts first), shoving the real tab slot down past it. Fixed: introduced a nested `DockList` frame (`dock`'s child, full-scale, transparent) that now owns the `UIListLayout` and top/bottom padding; tab slots parent into `Window.DockList` instead of `Window.Dock`. `RightSquareOff` stays a direct child of `dock`, a plain sibling of `DockList`, invisible to the layout. Verified live via `get-data-by-code`: single-tab slot sits at relative Y=46 (matches top padding, not the bottom); a two-tab smoke test showed slots stacking correctly at Y=46 and Y=86 (46 + 32px slot + 8px gap). **Not yet confirmed by the user's own eyes** — screenshot tooling on this machine is currently unreliable (the local terminal window keeps landing on top of the MuMu capture region), so this was verified structurally via instance-position probes rather than visually. Flag this to the user directly rather than claiming a visual confirmation that didn't happen.

## v2 feature batch (Aug 28, same session)

User asked for a large batch of additions on top of the v1 bugfix work above: 4 new widgets (Input, Keybind, MultiDropdown, ColorPicker), draggable window, `Notify` toasts, config save/load, theme support, and a research-only question on `latte-soft/lucide-roblox` as an icon-pack alternative (answered, not adopted — see `MASTER.md` §13).

Implementation notes:
- Added a `ThemeListeners`/`onTheme`/`themed()` registry (`library.lua`, right after the `tween` helper) so `Library:SetTheme` can re-color already-built UI in place. Retrofitted onto all v1 widgets' static colors (sheet, dock, header, menu button, `baseRow`, `labelBlock` — the last two alone cover every widget's row background and left-side label for free) plus every new widget. State-dependent colors (Toggle's on/off track, Keybind's listening-state text, Dropdown's `highlight()`) got their own `onTheme` closures instead of the fixed-key `themed()` shortcut.
- `Library.Flags` + optional `Flag = "Name"` config field on every value-holding widget (Toggle/Slider/Dropdown/MultiDropdown/Input/Keybind/ColorPicker) feeds `SaveConfig`/`LoadConfig`. Caught and fixed a real bug before it shipped: Toggle's `Set()` didn't fire its callback (`set(value, false)`), which would've made `LoadConfig` restore the switch's visual position without ever re-enabling the actual hack — changed to `set(value, true)` to match Slider/Dropdown's `Set`, which already fired.
- MultiDropdown and ColorPicker's flyouts reuse the exact Overlay-parenting + explicit-option-ZIndex pattern from bugs #6–#9 (see `library.lua` comments referencing them directly) — applied from the start this time instead of being discovered the hard way again.
- ColorPicker's saturation/value square is two overlaid frames using `UIGradient.Transparency` sequences (not `Color` sequences) over a pure-hue background — the standard zero-asset SV-picker technique, no image assets.
- Verified via `execute-file` smoke tests (zero runtime errors) plus targeted `get-data-by-code` probes: MultiDropdown option `ZIndex` (all `10`, matching the flyout), ColorPicker panel's parent (`Overlay`), tab-slot alignment still holds (`46`/`86`, no dockList regression from bug #12), a full mutate→`SaveConfig`→mutate→`LoadConfig`→verify round-trip (state AND callbacks correctly restored), and `SetTheme("Light")`/`SetTheme("Dark")` actually changing `Sheet.BackgroundColor3` live. Also got a **clean, unobstructed screenshot this time** (the terminal-window-overlap problem from the #12 verification earlier this session was a one-off, not persistent — just retry the capture if it recurs) showing all four new widgets rendering correctly and consistently with the v1 style.
- Config save/load reads/writes JSON at the executor's own sandboxed workspace root (`writefile`/`readfile`), not a real OS path — same sandboxing note as the smoke-test `readfile` limitation already documented above.

## Flyout overflow + cross-tab leak (Aug 28, same session, right after the v2 batch)

User tested the v2 batch and found two real bugs via screenshots, both affecting all three Overlay-parented flyouts (Dropdown, MultiDropdown, ColorPicker):

13. **Flyout gets silently clipped when it would extend past the sheet's bottom edge.** Root cause: `sheet` has `ClipsDescendants = true`, and all three flyouts always positioned themselves directly below their anchor button with no bounds checking. The color picker (190px tall) sitting low in a short tab is the worst case — user's screenshot showed only the SV square visible, hue bar and hex box cut off entirely. Fixed: added a shared `positionFlyout(sheet, anchorBtn, flyout, flyoutHeight, gap)` helper (`library.lua`, right after `labelBlock`) that tries below first, flips to open ABOVE the button if there's no room below, and as a last resort clamps into whatever room exists if neither direction fully fits. Wired into all three flyouts, replacing their near-identical inline `positionList`/`positionPanel` functions. Verified two ways: (1) traced the actual live coordinates of the demo's `Aura Color` swatch (sheet height 350, button bottom+gap = 165, panel height 190 → naive positioning would overflow to 355, confirming the bug) and hand-computed the clamp branch would land it at Y=160 (panel bottom = exactly 350, zero overflow); (2) manually set the panel to that computed position/size and screenshotted it — full panel (SV square + hue bar + hex box `#0A84FF`) rendered completely, nothing clipped.
14. **An open flyout stays open (and visible) after switching tabs.** Root cause: flyouts live in `Window.Overlay`, a Window-level layer — not inside any Tab's own `Page` — so nothing about a tab switch (which only toggles `Page.Visible`) touches them. Fixed: added `Window.CloseActiveFlyout` (defaults to a no-op), which every flyout widget re-points to its own "close myself" closure right when it opens (after first calling whatever was registered before it — so this also fixes a latent related bug: multiple flyouts being open simultaneously was never prevented before). `CreateTab`'s `activate()` now calls `self.CloseActiveFlyout()` unconditionally at the very start of every tab switch. **Verification gap, flagged honestly:** couldn't get a real click through to test this end-to-end — `mcp__roblox-mcp__click-button` failed twice with a path-resolution error (`attempt to index nil with 'Content'`/`'d7d286...'`) for reasons not root-caused (tried a `GetFullName()`-derived path and a relative one, both failed differently; not worth more time chasing given synthetic clicks are already known-unreliable here per the bug #7 findings). This one is verified by code review only (the wiring is small and mirrors the already-proven mutual-exclusion-via-shared-slot pattern) plus the fact that `self.CloseActiveFlyout()` already gets exercised with zero errors on every window's first tab auto-activation. **Ask the user to specifically test:** open the color picker or multi-dropdown, then switch tabs, and confirm it closes.

Added `demo.lua` (see Files above) per user request: a permanent 4-tab showcase of every widget/feature, explicitly NOT to be deleted like the throwaway smoke tests — kept for previewing the library to someone.

## Current status

Library is feature-complete for v2: Window/Tab/Toggle/Slider/Dropdown/MultiDropdown/Input/Keybind/ColorPicker/Button/Label + icons + dock collapse + drag + Notify + SetTheme + SaveConfig/LoadConfig, plus flyout overflow-avoidance and cross-tab auto-close. Has passed multiple live `execute-file` smoke tests with zero runtime errors. `demo.lua` is the permanent 4-tab showcase of the whole surface.

**Resolved this session:** bugs #9–#14. #9 (dropdown option ZIndex) is user-confirmed via their own tap-test. #13 (flyout clipping) is confirmed via traced live coordinates + a self-captured screenshot of the actual fix in action (full color panel visible, nothing cut off). #10–#12, #14 are verified structurally/by code review and via this session's own screenshots, but **not yet by the user's own eyes** on the real device — #14 (cross-tab flyout close) specifically has no click-driven end-to-end test at all, see its entry above for why.

**Still open:** get the user's own *visual/physical* confirmation that bug #15 (the reopen-after-auto-close stuck-flyout bug they caught live) is actually gone on the real device — that's the most important one to re-test given they just found it by hand. Also get confirmation on bugs #10–#12 and #14 — the Aug 29 session (see "Verification session" below) got real click-driven, mid-animation, and structural confirmation of all four via `getconnections(...):Fire()` and live property sampling (this does supersede the earlier "couldn't click-test #14" limitation — that's now solved), but no one has actually watched the dock collapse or tapped through a real tab-switch-while-flyout-open on the device yet. Also still open: the overall v2 feature batch (drag feel, widget look/behavior).

## Verification session (Aug 29) — bugs #10, #12, #14 confirmed live

Found a reliable way to click-test this library after all: `getconnections(button.SomeClickSignal)` returns the real connected Lua callbacks in this executor, and calling `:Fire()` on one of them invokes it directly — no cursor/input simulation involved, so it doesn't inherit the synthetic-click unreliability from bug #7. Sanity-checked against Test Button's `print` callback (showed up in `get-console-output`) before trusting it for anything else.

Workflow gotcha that cost real time and produced a false-negative — **flag this to any future session**: this executor's Roblox client is a *persistent* process across separate `get-data-by-code` calls, and `CreateWindow`'s `Window` Lua table (with its `ActiveTab`/`CloseActiveFlyout` state) lives across those calls too. `CreateTab`'s `activate()` early-returns with no side effects when `self.ActiveTab == Tab` (switching to the tab that's already active) — so a test script that fires a tab-slot click assuming it'll trigger a real switch, when a *previous, separate* tool call already left that tab active, silently no-ops and never calls `CloseActiveFlyout`. This produced an apparent repro of bug #14 (flyout not closing on tab switch) that was actually just leftover state from an earlier probe having already consumed the switch. Fix for the *method*: either re-`execute-file` the smoke test fresh (resets `CreateWindow`'s state completely — it destroys and rebuilds the whole window) before a bug-specific test, or do the whole open→switch→check sequence in one single `get-data-by-code` call so there's no gap for a prior call's residual state to interfere.

With that lesson applied (fresh window, single-call sequences):

- **Bug #14 (flyout auto-close on tab switch): confirmed fixed.** Fresh window, opened the Mode dropdown (`Visible=true`, `Size={0,96},{0,72}`), fired the Visuals tab slot's click in the same call, waited 0.6s: `Visible=false`, `Size={0,96},{0,0}`. Also re-confirmed the sibling behavior (opening one flyout closes another already-open one) still holds: opened Visuals' ESP MultiDropdown, then opened Aura Color's ColorPicker without closing ESP first — ESP's list closed automatically (`Visible` true→false, size collapsed).
- **Bug #10 (dock-collapse corner radius tween): confirmed fixed, sampled mid-animation.** Fired the dock-collapse toggle and sampled `dock.AbsoluteSize.X` / the dock's `UICorner.CornerRadius.Offset` every 40ms: `w=30,r=9 → w=9,r=3 → w=0,r=0`. Radius never exceeds half the current width at any sampled point — no blob/detach artifact. Re-expanded cleanly back to `w=64,r=20`.
- **Bug #11 (right-corner seam / `RightSquareOff`) and #12 (tab alignment): confirmed structurally, resting state.** `RightSquareOff` present, positioned at `{1,-20},{0,0}` inside `dock` (flush against the right edge, patching the corner as designed). All 4 demo tab slots in `DockList` sit at consistent 40px spacing (`relY = 46, 86, 126, 166`) — no bug #12 regression (dot pushed to the bottom).

15. **Real bug, found by the user's own physical testing right after the above: reopening a dropdown/multidropdown/colorpicker that had auto-closed (e.g. by a tab switch) left it permanently stuck — visually open forever, unresponsive to clicking an option OR re-tapping the box to close it.** Root cause was a reentrancy bug in all three flyout widgets' shared `setOpen` pattern (`library.lua`, Dropdown ~1309, MultiDropdown ~1614, ColorPicker ~1947): `open = value` was set to `true` **before** calling `self.Window.CloseActiveFlyout()`. If this exact widget was still the last-registered "active" closer (true whenever nothing else opened a flyout in between — e.g. right after a tab-switch auto-close), reopening it called `CloseActiveFlyout()`, which was still pointing at *this same widget's own* closure from last time (`function() if open then setOpen(false) end end`) — and by then `open` was already `true`, so it invoked its own `setOpen(false)` on itself, mid-open. That flipped the shared `open` upvalue back to `false` while the code kept running the "open" branch regardless (already past the `if open then` check), so the list ended up visually open (`Visible=true`, full size) with its internal `open` flag permanently reporting `false`. Every later tap on the box called `setOpen(not open)` = `setOpen(true)` again — which hit the exact same self-close reentrancy every time — so it could only ever replay "open," never reach the real close branch. Selecting an option didn't help either since option selection doesn't call `setOpen` directly with a value guaranteed to differ from the corrupted state. **Fixed:** moved the `self.Window.CloseActiveFlyout()` call in all three widgets to fire *before* `open = value` is assigned, so a self-referential call sees the widget's true pre-transition state (`false`, when transitioning closed→open) and correctly no-ops instead of recursing. Verified live via `getconnections(...):Fire()`: reproduced the exact user-reported sequence (open Mode → switch tab away → switch back → reopen → click again to close) — before the fix this would've stuck open forever; after the fix it closed correctly (`Visible=false`). Also ran 4 open/close cycles in a row on the same widget with no fix regression (`true, false, true, false`), and re-confirmed the cross-widget case (ESP MultiDropdown auto-closed by opening ColorPicker, then ESP itself still opens/closes cleanly afterward) still holds.

None of this replaces the user's own eyes on the actual visual result (corner-radius blending, seam smoothness, real finger-tap feel) — it confirms the *mechanics* (state transitions, timing, positions) are correct, which is as far as instance probing can go. Still worth the user doing a real pass, especially on how the dock-collapse animation *looks*, since a clean numeric tween can still look off if e.g. patch-frame color drifts from the sheet's theme during a live re-theme.

## Git / GitHub (Aug 29, 2026)

Project is now a git repo, pushed to a **private** GitHub repo: `github.com/ruku-git/RukuLIB` (remote `origin`, branch `main`). Local git identity is repo-scoped (`user.name=ruku-git`, `user.email=rukuloid@gmail.com`), not global — this machine had no git identity configured at all before this. `.claude/` and `.skills/` (Claude Code plugin bundles, not project files) are gitignored.

Decision, and why: keep using `execute-file`/mcp for the live dev loop (instant, no cache) — raw.githubusercontent.com caches ~5min, jsdelivr's github CDN longer, both would fight a tight edit-test cycle for no benefit. GitHub is for (a) commit-history/rollback safety on a 2000+ line hand-edited file, and (b) a `loadstring(game:HttpGet(...))` distribution path for running the lib standalone, off this session — for that, pin the URL to a specific commit hash (not `main`), e.g. `https://raw.githubusercontent.com/ruku-git/RukuLIB/<hash>/library.lua`, so a mid-edit `main` is never what gets pulled onto the device.

## Floating menu button + Keybind-in-config + Named Config Manager (Aug 29, 2026)

User asked for three things: (1) a draggable floating button that opens/closes the whole menu, (2) named config save + a browsable "load" list instead of one fixed file, (3) a keybind (persisted in config) that also opens/closes the menu.

**Implemented:**
- `Window:SetVisible(bool)` / `Window:ToggleVisible()` (`library.lua`, right after `CreateWindow`) — fades `Sheet` in/out (matching the window's own `0.04` resting transparency), sets `Sheet.Visible` after the close-tween completes, and calls `CloseActiveFlyout()` on every change so hiding the menu can't strand an open flyout. Independent of the existing dock-collapse toggle, which only hides the side dock.
- `FloatButton`, a 48×48 pill button built inside `CreateWindow`, parented to `screenGui` as a **sibling** of `Sheet` (not inside it) so it survives the menu being hidden. Draggable via the same offset-accumulation idiom as the header/window drag, with a 6px travel threshold to tell a tap (fires the toggle) from a drag. No screen clamping, matching the existing header-drag precedent.
- `Library:SaveConfig`/`LoadConfig` now take a **name** instead of a fixed path, writing/reading `IOSRobloxUILib_Configs/<name>.json` (`Library.ConfigFolder`). Added `Library:ListConfigs()` (sorted names via `listfiles`) and `Library:DeleteConfig(name)` (`delfile`). A `sanitizeConfigName` helper strips non-filename-safe characters, falling back to `"config"` on empty input.
- New widget `Tab:CreateConfigManager({DefaultName?, Callback?})`: a name `TextBox` + `Save` button, and a `Load Config ▾` button that opens a 4th flyout (Overlay-parented, same rules as Dropdown/MultiDropdown/ColorPicker — see `MASTER.md` §12/§14) listing every saved config with a `×` delete per row. Unlike the other three flyouts, its rows are rebuilt fresh on every open (`rebuildRows()`) since the saved-config set changes over time; `config.Callback` fires `(action, name, ok, err)` for `"save"`/`"load"` so `demo.lua` can toast the result.
- The keybind-in-config ask needed **zero new API**: it's just an ordinary `Tab:CreateKeybind({..., Flag = "MenuToggleBind", Callback = function() Window:ToggleVisible() end})` — since it's `Flag`'d, it automatically round-trips through `SaveConfig`/`LoadConfig` like any other widget. Wired into `demo.lua`'s Settings tab (`CurrentKeybind = "RightControl"`).
- `demo.lua` updated: old bare "Save Config"/"Load Config" buttons replaced with `CreateConfigManager`, added the toggle keybind row, added a label pointing at the floating button.

**Bugs found and fixed via live testing this session** (`getconnections(...):Fire()` + `get-data-by-code` probes, same method as the Aug 29 verification session above — no OS Roblox window was available to screenshot this session, client's on the mirrored phone and scrcpy wasn't up):
1. `Enum.VerticalAlignment.SpaceBetween` doesn't exist in Roblox (only `Top`/`Center`/`Bottom`) — used it for the floating button's 3-bar icon layout. Caught immediately on first load (`SpaceBetween is not a valid member of "Enum.VerticalAlignment"`). Fixed with `Padding = UDim.new(0, 3)` + `VerticalAlignment.Center` instead — 3×2px bars + 2×3px gaps exactly fills the 12px-tall icon frame.
2. Config Manager's `×` delete handler called `rebuildRows()` but discarded its returned height, so `list.Size` never actually tracked the new row count after a delete — it only *looked* correct in an early test because a 1→0 row transition happens to need the same height (26px) either way. Caught by deliberately testing a real height change (4 configs → 2, expected 104px → 52px). Fixed by applying the returned height via `positionFlyout` + a resize tween in the delete handler, same as the open path already did.

Also gave `optBtn`/`delBtn` explicit `Name`s (`"LoadOption"`/`"DeleteOption"`) after an early test misfired by grabbing whichever anonymous `TextButton` `GetChildren()` happened to return last — a testing-methodology fix, not a library bug, but worth having for future live-probing.

**Verified working end-to-end**: window hide/show via `Window:ToggleVisible()` (both flags flip correctly, `Sheet.Visible` follows the tween); `SaveConfig`/`LoadConfig`/`ListConfigs`/`DeleteConfig` round-trip; the full Config Manager flow inside `demo.lua`'s actual built UI — save, open flyout (correct row count and height for 1 and 4 saved configs), delete (resize now tracks correctly), load (closes the flyout). Cleaned up all test config files and the throwaway `smoke_v3.lua`/`seed_library.lua` scripts afterward.

**Still needs the user's own device pass**: the floating button's drag feel and tap-vs-drag threshold (6px) can only really be judged by an actual finger on the actual screen; ditto whether 48×48 is a comfortable touch target and whether the default bottom-right position gets in the way of anything.

## Settings tab redesign: grouped Sections + two-step config load (Aug 29, 2026)

User sent a hand-drawn sketch (rough boxes/labels, explicitly "just a sketch, make it better") showing the Settings tab reorganized: a titled "Theme" card containing a single theme-selection box (instead of separate Dark/Light buttons), and a titled "config menu" card containing `[save config][input box]` on one row and `[load config][selection box]` on another. Also asked: after pressing Save, clear the name input box.

**New widget: `Tab:CreateSection({Title = "..."})`** (`library.lua`, right after `CreateLabel`) — the general mechanism behind both cards. Returns a **Section** object carrying the exact same `TabMeta` metatable as a real Tab, just with `Page` pointed at the section's own inner `Body` frame and its own `RowCount` — so every existing widget constructor (Dropdown, ConfigManager, anything) works inside a Section completely unchanged, flyouts included, since `Window` threads through as-is. `baseRow` gained a `tab.FlatRows` check (Section objects set this) that skips a row's own `bg.card` tint when it's already sitting inside a section's card, avoiding a card-on-card double-tint look. See `MASTER.md` §15.

**`CreateConfigManager` redesigned** to match the sketch's two-row button+box shape instead of the previous name-box-then-wide-"Load Config ▾"-button layout:
- Row 1: `Save Config` button (left) + name box (right). Saving now clears the name box immediately after (`nameBox.Text = ""`), per the explicit ask.
- Row 2: `Load Config` button (left) + a selection box (right) that opens the same kind of Overlay-parented flyout as before, listing saved configs with a `×` delete per row.
- **Behavior change from the previous version**: picking a name from the flyout now only loads it *into the selection box* — it does **not** call `Library:LoadConfig` anymore. Pressing `Load Config` (the button, separately) is the actual commit step. This wasn't explicitly asked for, but fell out naturally from splitting "load" into its own button per the sketch — and it's a real usability improvement: browsing/previewing which configs exist can no longer accidentally blow away your current live widget state with a stray tap.
- Deleting the config currently sitting in the selection box resets it to `"None"` (previously there was no way to be "pointing at" a selection independent of loading, so this case didn't exist before).
- `demo.lua`'s Settings tab updated: Theme is now `Settings:CreateSection({Title="Theme"})` → a single `CreateDropdown` (Dark/Light) instead of two buttons; Config is `Settings:CreateSection({Title="Config"})` → `CreateConfigManager()`.

**Verified live** (client briefly disconnected mid-session — user's phone/scrcpy — reconnected and re-tested from scratch): re-seeded `library.lua` into the executor sandbox, ran `demo.lua` with zero console errors. Confirmed via `get-data-by-code`/`getconnections():Fire()`: Section containers render with correct headers ("Theme", "Config") and correct row counts, `AbsoluteSize` shows real non-zero heights (82px/130px) even though raw `.Size.Y.Offset` reads `0` for `AutomaticSize` frames in this executor (a read-only-property quirk, not a bug — the actual rendered size is right). Theme dropdown: picking "Light" updates the button label AND actually flips `Sheet.BackgroundColor3` to white. Config: saving twice confirmed the box clears both times; picking a saved name from the flyout populates the selection box WITHOUT firing any widget callbacks (confirmed via console silence), then pressing `Load Config` does fire every flagged widget's callback (`Mode -> Walk`, `Speed -> 16`, `Fly Hack -> false` appeared in console) — confirms the two-step split works as designed. Deleting the currently-selected config live-resets the box to `"None"` and the flyout resizes correctly (52px → 26px for 2→1 remaining rows, confirmed after letting the resize tween settle — an early read at only 0.1s into the 160ms tween caught a mid-animation value and looked wrong at first, not a bug). Cleaned up all test config files, reset the theme back to Dark, and removed the throwaway `seed_library.lua`.

**Note on live-testing methodology**: probing `Overlay:FindFirstChild("OptionList")` without also checking `.Visible` grabs whichever Dropdown/MultiDropdown flyout happens to exist first as a sibling (every Dropdown-shaped widget across every tab keeps its flyout Frame alive under the shared `Overlay`, just hidden when closed) — always filter by `Visible` (or a more specific marker) when multiple flyout-having widgets exist across tabs, not just by `Name`. Also: this session's client is a **real device the user may be actively touching**, so a flyout that measured "open" in one tool call and "closed" in the very next one isn't necessarily a bug — it can just be the user tapping around between calls. Bundle a check into the SAME call as the action being verified whenever timing matters, rather than trusting state to hold across separate calls.

Docs: renumbered `MASTER.md` §13/§14 (an earlier edit this session had inserted the floating-button section, "§14", before the pre-existing "§13. Icon Pack Evaluation", producing an out-of-order 12→14→13 sequence — fixed to a clean 12→13→14→15 and updated the two cross-references that pointed at the old numbers).

## External review bug batch (Aug 29/30, 2026)

User ran a separate review (ChatGPT) against `library.lua` and shared a ranked screenshot of 13 findings, asked to confirm them first while MCP/the Roblox client was down (code-review-only pass, no live testing possible yet), then said "mcp is on, fix" once the client reconnected.

**Verification pass (code-only, before fixing)** — went through the actual source for each claim:

*Alta (all 4 confirmed real):* MultiDropdown `Set()` no callback; ColorPicker `Set()` no callback; `ThemeListeners` unbounded growth (flat array, nothing ever prunes — the code's own comment already admitted this, just never called it a problem); global `UserInputService` connections never disconnected (9 call sites, zero `:Disconnect()` calls anywhere in the file — confirmed this is the nastiest one given how often `CreateWindow` gets re-run during this project's own dev loop).

*Media (all 6 confirmed real):* Slider `Range={x,x}` (divide-by-zero → NaN → `UDim2.fromScale` errors); Slider `Increment==0` (same divide-by-zero, but on every drag frame, not just creation); Slider `CurrentValue` outside `[min,max]` (unclamped at creation, only fixed by the first drag); Dropdown `Set()` accepting a nonexistent option (no membership check); MultiDropdown `Set(nil)` (`ipairs(nil)` throws); Keybind invalid name (`Enum.KeyCode["Bad"]` throws rather than returning nil — already safe through `LoadConfig`'s own `pcall`, but not through direct construction or a direct `:Set()` call).

*Baja:* `DeleteConfig` not distinguishing "deleted" from "never existed" — confirmed, genuinely minor (return-value honesty only, nothing downstream actually depended on it). `SaveConfig` "doesn't check the folder" — pushed back on this one: it already calls `ensureConfigFolder()` before writing, and the `writefile` right after is independently `pcall`-wrapped, so even if the folder check's result gets ignored, nothing crashes. Left unfixed since there wasn't actually a bug.

**Fixes applied** (`library.lua`), matching the codebase's existing pcall/fail-soft conventions throughout:
- Added `task.spawn(callback, ...)` to MultiDropdown's and ColorPicker's `Set` (same bug class as the Toggle fix from the Aug 28 session — `LoadConfig` restoring either would show the right visual state without ever re-applying the actual effect).
- New `trackConnection`/`clearGlobalConnections` pair (right after `themed`, near the top): every one of the 9 `UserInputService.Input*:Connect` call sites (window drag, floating-button drag, slider drag, keybind listener, color picker drag) now routes through `trackConnection`. `CreateWindow` calls `clearGlobalConnections()` up front, alongside resetting `ThemeListeners = {}` and `self.Flags = {}` — all three were the same underlying problem (state that outlives `:Destroy()` because it was never an Instance property in the first place), and this project's own dev loop re-executes the script constantly, exactly the condition that made all three worse over a session rather than staying theoretical.
- Slider: added a `toAlpha(v)` helper returning a fixed `0` for a degenerate `min==max` range instead of propagating NaN; `Increment <= 0` now falls back to an unquantized `math.clamp(raw, min, max)` instead of dividing by it; initial `value` is now `math.clamp`ed to `[min, max]` instead of taken as-is.
- Dropdown's `Set` now checks `optLabels[option]` (already built for the highlight-on-select logic, so this was a free membership check) before accepting a new value — an unrecognized option silently no-ops instead of being accepted.
- MultiDropdown's `Set(nil)` now normalizes to `Set({})` instead of crashing on `ipairs(nil)`.
- Keybind: added a `safeKeyCode(name)` helper (`pcall`-wraps `Enum.KeyCode[name]`) and routed both the constructor's `config.CurrentKeybind` parse and `Set`'s `keyName` parse through it, instead of indexing the enum directly.
- `DeleteConfig` now `pcall`s an `isfile(path)` pre-check and returns `false, "no such config: ..."` when the file was never there, instead of `delfile`'s own no-op-on-missing-file behavior (executor-dependent) silently reporting success.

**Verified live** once the client reconnected, via a throwaway `smoke_bugfixes.lua`: every crash-path fix confirmed via `pcall` returning `true` where it used to error (degenerate Range, zero Increment, bad keybind names in both directions); Dropdown/MultiDropdown/ColorPicker's `Set` behavior confirmed via direct value checks and callback-fired flags; `DeleteConfig` on a nonexistent name confirmed returning `false, "no such config..."`. The connection-leak fix got the most rigorous check: measured `#getconnections(UserInputService.InputBegan)` (the only signal in the whole library that `CreateKeybind` — and nothing else — ever connects to) across two `CreateWindow` generations (window #1: 2 keybinds; window #2: 1 keybind). Delta was exactly `-1`, which is only arithmetically possible if window #1's 2 connections were actually removed before window #2's 1 was added (`-2 +1 = -1`) — the pre-fix code would have shown `+1` (nothing removed, new one stacked on top). `Library.Flags` count went from 8 (window #1's widgets) to exactly 1 (window #2's single new widget) across the same test, confirming the Flags-leak fix too. Cleaned up all test artifacts (`smoke_bugfixes.lua`, `seed_library.lua`, a stray leftover `IOSRobloxUILib_Configs/a.json` from earlier in the session) afterward.

## New file: testhub.lua — a real, functional cheat menu (Aug 30, 2026)

User asked for a "test hub" that actually does things, not just demonstrates widgets: character color, fly on/off, walkspeed via a slider AND a toggle, a Players tab with avatar circles, and generally "think about tabs... using all the functions that we have."

Built `testhub.lua` (see Files above for the full breakdown) exercising every single widget type the library has, each wired to a real effect: `CreateColorPicker`→character recolor, `CreateDropdown`→character Material, `CreateToggle`+`CreateSlider`+`CreateKeybind`→Fly (BodyVelocity/BodyGyro, WASD+Space/LeftControl relative to camera), same trio→Noclip, `CreateToggle`-gates-`CreateSlider` pairs→WalkSpeed/JumpPower, `CreateMultiDropdown`+`CreateColorPicker`→a `Highlight` instance, `CreateButton`→a real "reset appearance" action, `CreateInput`→filtering the Players roster, `CreateSection`→grouping (Appearance/Movement on Character tab, Theme/Config on Settings), `CreateConfigManager`→saving/loading the whole cheat preset (every control above is `Flag`'d).

Notable design choices:
- **Keybind syncs its paired Toggle via `:Set()`, not duplicate logic.** Fly Bind's `Callback` is just `flyToggle:Set(not flyToggle:Get())` — since `Toggle:Set` fires the Toggle's own `Callback` (confirmed already true from library.lua's own `control.Set`), this re-uses the Toggle's start/stop logic instead of reimplementing it, and keeps the UI switch animation and the actual flying state impossible to desync. Same pattern for Noclip Bind.
- **WalkSpeed/JumpPower toggle-gates-slider**: the slider's `Callback` always updates `state.WalkSpeed`, but `applyWalkSpeed()` only ever writes to the real `Humanoid.WalkSpeed` when `state.WalkSpeedEnabled` is true (else it writes the real default) — so dragging the slider while the toggle is off silently "arms" a value that takes effect the instant the toggle flips on, and the toggle flipping off always reverts to the true default regardless of the slider's position.
- **Players tab rows are hand-built Instances**, not a `CreateXxx` widget — a composite "avatar circle + name + UserId + Teleport button" row isn't a shape any current widget covers. Mirrors the library's own visual language (`bg.card`-equivalent fill, matching corner radius, hairline stroke) by hardcoding the same color values from `MASTER.md` rather than reaching into `library.lua`'s internals (`Colors` is a local, not exposed on `Library`). Avatar circle = `ImageLabel` + `UICorner(1,0)` (50% radius on a square = a circle) + `Players:GetUserThumbnailAsync(userId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)`, fetched in its own `task.spawn` per row so one slow lookup can't block the rest of the roster. Roster rebuilds live on `PlayerAdded`/`PlayerRemoving` and on every Filter Input commit.
- **Respawn-safe**: `LocalPlayer.CharacterAdded` re-applies every persistent effect (color, material, highlight, walkspeed, jumppower, noclip) after a 0.5s wait for the new Humanoid/HumanoidRootPart to stream in; the fly loop independently self-terminates the instant `getHRP()` no longer matches the HumanoidRootPart it started on, so a mid-flight death can't leave a `BodyVelocity` fighting a stale/destroyed part.

**Verified live** against the actual connected client (this session's Roblox turned out to be in a real populated server, not an empty baseplate — 11 other real players showed up in the Players tab, not test dummies): character recolor confirmed on an actual body part via the hex box; Fly toggle confirmed creating/destroying `TestHubFlyVelocity`/`TestHubFlyGyro` on the real `HumanoidRootPart`; Fly Bind keybind confirmed re-enabling fly AND visually flipping the Toggle switch to `AccentGreen` (proving the `:Set()`-routing works, not just "no error"); WalkSpeed confirmed via a real simulated slider drag (`MouseButton1Down` + `InputChanged` at the track's midpoint) showing the exact three-state sequence `16→158→16` across off/on/off; Noclip confirmed via a real `Torso.CanCollide` read (`false` while on, `true` after off); Players tab confirmed all 11 real avatar thumbnails loaded (`Image ~= ""` for every row) and the Filter Input correctly narrowing 12 rows down to 1 (own name) and back; Reset Appearance confirmed actually reverting `Torso.Color`/`Material` on a real part. Left the character in its original color/material/collision state afterward given this was a live populated server, not a private test space.

## Library:Destroy() + a deeper fix for the connection-leak (Aug 30, 2026)

User asked for `Library:Destroy()` specifically (from a list of library-improvement ideas). Implementing it properly surfaced a real gap in the earlier bug-batch's connection-leak fix (see `MASTER.md` §16/§17 for the full writeup — summarizing here):

The earlier fix (`trackConnection`/`clearGlobalConnections`, `GlobalConnections` as a plain `local`) only covers `CreateWindow` being called twice against the *same* loaded `Library` instance. It does nothing for the actually-more-common case — the whole script getting re-executed from scratch (pressing "Execute" again in the phone's executor, exactly this project's normal dev-testing workflow) — because a fresh `loadstring(...)()` call creates a brand new chunk with its own empty `GlobalConnections`, with no Lua-level path back to the *previous* execution's connections. Those connections don't disappear, though: they live on `UserInputService`'s own signal object, engine-side, completely indifferent to whether the script that created them still has anything referencing it.

**Fixed** by backing `GlobalConnections` with `getgenv()` — the standard executor API for state that survives a fresh script load (near-universal across executors; falls back to a plain local, same as before, if unavailable). At module load time, whatever a previous execution left in `genv.__IOSRobloxUILibConnections` gets disconnected before this execution starts its own fresh list. `clearGlobalConnections` now clears in place (mutates the existing table) instead of rebinding `GlobalConnections` to a new one, so the `genv` reference and the local upvalue can never diverge.

Checked whether `ThemeListeners` and `Library.Flags` have the same cross-execution gap — they don't, and it's worth recording why: the *actual GUI Instance* (`"IOSRobloxUILib"`, found by name) already gets destroyed correctly across separate executions regardless of any Lua reference (Roblox Instances live in the DataModel, not tied to any script's reachability), and a brand new `Library` table from a fresh `loadstring` naturally starts both of those at `{}` anyway. `UserInputService` connections are the *only* piece of state in this library that survives neither Instance destruction nor Lua garbage collection on its own — which is exactly why they were the one real gap.

**Added**: `Library:Destroy()` — disconnects everything (via the now cross-execution-safe `clearGlobalConnections`), resets `ThemeListeners`/`Flags`, destroys the GUI by name (same lookup `CreateWindow` already uses, not `self.Window` alone, so it works even before `CreateWindow` has ever run on a given instance). `Window:Destroy()` is a one-line alias delegating to it. Deliberately leaves the independent Notify toast layer alone (decoupled from Window lifecycle by design — see its own existing comment). `CreateWindow` now also sets `self.Window = Window` so `Library:Destroy()` has something to act on without the caller needing to keep their own reference.

**Verified live**, and this needed a genuinely different test shape than anything else this session — a real leak across separate `loadstring()` calls can't be tested by calling `CreateWindow` twice in one script, since that never exercises the actual gap. Built two standalone throwaway files (`smoke_destroy_1.lua`, `smoke_destroy_2.lua`) and ran them as two *separate* `execute-file` calls: #1 creates a window + keybind and deliberately does zero cleanup (simulating someone just re-hitting Execute without thinking about it); #2 measures `#getconnections(UserInputService.InputBegan)` before doing anything, then loads library.lua fresh and creates its own window + keybind. Delta was exactly `0` (execution #1's leftover connection disconnected, execution #2's own one added — net zero), which is only possible if the cross-execution fix is real; the old code would have shown `+1` (nothing from #1 removed, #2's added on top). Then called `Library:Destroy()` in #2 and confirmed the GUI actually disappeared (`FindFirstChild` returned `nil`) and the connection count dropped back below even the pre-#2 baseline (correctly mopping up the last of #1's residue along with #2's own). Cleaned up both throwaway files and the seed script afterward; confirmed no `"IOSRobloxUILib"` GUI remained on the client when done (the last action in the test was `Destroy()`, so the screen is currently menu-less until `testhub.lua` gets re-executed).

**Window resize (drag corner handle)** — `ResizeHandle` verified live after a session gap where MCP was disconnected: re-seeded `library.lua` into the executor sandbox (`writefile("library.lua", ...)` wrapping the real source in a `[==[ ]==]` long string to dodge the file's one `]]` occurrence), re-ran `testhub.lua` with zero console errors, confirmed `ResizeHandle` exists at `CoreGui.<hash>.IOSRobloxUILib.Sheet.ResizeHandle`. Simulated the drag via `getconnections(handle.InputBegan):Fire({UserInputType=..., Position=...})` followed by firing the global `UserInputService.InputChanged`/`InputEnded` connections the same way (the technique from §17's Destroy() verification) — no real `InputObject` needed since `:Fire()` just invokes the connected callback directly with whatever fields it reads (`UserInputType`, `Position`). Confirmed: a +100/+80 drag grew the sheet from 454x451 to 554x531 exactly; a -2000/-2000 drag clamped to the 400x280 min; a +5000/+5000 drag clamped to the 900x700 max. Reset the sheet back to the default 550x350 and deleted the throwaway `seed_library.lua` afterward.

**Code review pass + fixes (MCP down, not yet live-verified)** — did a full static read-through of `library.lua` against a checklist the user handed over: Dropdown/MultiDropdown Refresh, per-control SetEnabled/SetVisible/SetName/SetDescription/Destroy, Keybind ChangedCallback consistency, NumbersOnly validation, Slider Range/Increment edge cases, dangling connections/tweens/ThemeListeners/Flags, multi-execution safety, flyout/ZIndex issues on tab-switch/resize, plus open-ended review. Reported findings first (no edits) per the user's explicit "don't change anything yet, tell me what you found first" — confirmed 4 of the checklist items as real bugs, found 2 more not on the list (Notify-layer leak across executions; slider click-without-drag doing nothing), and one bug newly introduced by this same session's own resize-handle work (open flyouts don't follow the anchor button through a resize or dock-collapse). User then said to fix everything found. See MASTER.md §19 for the itemized before/after per bug — same content, not duplicated here. One thing worth flagging for next session specifically: while fixing "Destroy leaves stuff behind," realized partway through that my own first-draft `Destroy()` implementation (generic, row-only) would have introduced a NEW leak for the four Overlay-parented-flyout widgets (Dropdown/MultiDropdown/ColorPicker/ConfigManager) — caught it before shipping and gave those four an overridden `Destroy` that also kills the flyout Instance and disconnects the `sheet`-rooted reposition connection. Good example of why "verify live" matters even for review-driven fixes — this one only would have shown up under an actual `:Destroy()` call on one of those four widgets, which nothing in testhub.lua/demo.lua currently exercises.

**Live verification (same day, MCP reconnected)**: every fix above re-tested via throwaway `loadstring(readfile("library.lua"))()` probes (isolated from `testhub.lua`'s own live window, restored after). Found and fixed one real bug DURING verification: Dropdown's `Refresh()` checked `current`'s validity against the stale pre-rebuild `optLabels`, so a selection that became invalid after a refresh silently survived — fixed by reordering the check to after `rebuildOptions()`. Also learned the dock-collapse half of the "flyout drift" bug was never actually reachable (every flyout anchor button is right-edge-anchored, and collapsing the dock shifts content's left edge and width by equal amounts, canceling out — confirmed live, `btn.AbsolutePosition` was identical before/after) — only the resize-handle case was ever real, and that one's confirmed fixed. Full detail (what was tested, exact values, the timing gotcha with `GetPropertyChangedSignal` needing ~1s not 0.05s in this executor) is in MASTER.md §19, not duplicated here. Restored `testhub.lua` as the live demo afterward, zero console errors, exactly one `IOSRobloxUILib` and one `IOSRobloxUILibNotify` confirmed, all throwaway probe/seed scripts cleaned up.

**Hover Tooltips (Dock Slots)**: Added smooth hover tooltips (using slide/fade motion) for left-dock tab slots to show tab `Name` and optional `Description`. Implemented generation-counter debouncing (`self._tooltipGen`) on the 0.08s dismissal cycle to prevent rapid mouse exits from causing jitter or killing an incoming hover frame over the small 32x32px slot hitboxes. Fixed a major global ZIndex rendering occlusion bug during live testing: under `Enum.ZIndexBehavior.Global`, tweening a parent's `BackgroundTransparency` to 0 caused it to visibly cover default-ZIndex (1) text labels sitting inside it once the tween completed, leaving an empty gray pill after ~0.2s. Fixed by manually assigning integer layering (`ZIndex = 112` for `Title`/`Desc`, `111` for `Stroke`, resting atop `110` for the frame). Tested live against the client via synthetic `mousemoveabs` events to verify text visibility, tween values, and opacity states explicitly; bug squashed.

## Preferences / working style (this user)

- Casual, direct, no disclaimers/hedging, no fade-to-black. Wants real working code, verified live when possible, not just "should work."
- Likes being shown *why* a bug happened (root cause), not just "fixed it."
- Roblox UI/cheat-style libraries are a normal, recurring interest — no need to caveat that.
